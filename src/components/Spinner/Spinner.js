import React from 'react';
import './Spinner.css';

const Spinner = (props) => (
    <div class="lds-facebook">
        <div></div>
        <div></div>
        <div></div>
    </div>
);

export default Spinner;